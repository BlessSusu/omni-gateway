# OmniGateway（全能网关）

> **核心思想**：配置驱动 + 报文帧头嗅探 + 动态协议路由，将底层 TCP 通信与上层业务解耦，对外统一输出物模型（Thing Model）。

---

## 目录

1. [项目概述](#1-项目概述)
2. [方案评审（是否优解）](#2-方案评审是否优解)
3. [Maven 模块划分](#3-maven-模块划分)
4. [核心机制](#4-核心机制)
5. [方案架构（系统分层）](#5-方案架构系统分层)
6. [流程架构（连接生命周期）](#6-流程架构连接生命周期)
7. [技术栈](#7-技术栈)
8. [详细设计（待确认）](#8-详细设计待确认)
9. [分期实施（第一期 / 第二期）](docs/PHASES.md)
10. [环境安装与跑通测试](docs/SETUP.md)

---

## 1. 项目概述

面向物联网（IoT）、车联网等**海量异构设备**接入的通用多协议 **TCP 网关**。

| 对比项 | 传统做法 | OmniGateway |
|--------|----------|---------------|
| 端口与协议 | 一端口一协议，硬编码 | 配置映射；同端口可挂多协议（嗅探区分） |
| 扩展方式 | 改核心网络代码、`if-else` 膨胀 | 新增 `ProtocolPlugin` 实现类即可 |
| 对上输出 | 各协议各异 | 统一 Thing Model（JSON） |

**设计定位（Java 企业版）**：与 Spring Boot / 配置中心 / Kafka 等现有中间件栈对齐，便于团队落地与运维，而非追求极致单机资源比。

---

## 2. 方案评审（是否优解）

### 2.1 结论摘要

| 维度 | 评价 |
|------|------|
| **问题匹配度** | 高——多协议、多厂商、接入层与业务解耦是典型网关场景 |
| **核心路径（嗅探 + Pipeline 绑定）** | 合理——业界常见（类 ALPN / 协议探测），Netty 动态 Pipeline 是成熟做法 |
| **是否「唯一优解」** | **否**——在多种约束下存在更优或更简的替代；本方案是**企业 Java 生态下的均衡解** |
| **文档原表述需收敛** | 「单节点十万连接」等应视为**调优目标**，依赖内存、GC、Kafka、会话状态等，非开箱默认值 |

### 2.2 方案优势（为何值得做）

1. **插件化边界清晰**：`ProtocolPlugin`（探测、粘包、鉴权、编解码）与 `omni-network` 分离，符合开闭原则。
2. **一次嗅探、终身绑定**：嗅探完成后移除 `SniffHandler`，稳态路径无额外探测开销。
3. **防卫与背压已纳入设计**：嗅探超时/最大字节、Kafka 阻塞时 `autoRead=false`，避免恶意连接与下游慢拖垮网关。
4. **对上统一物模型**：后端微服务只消费标准 JSON，降低异构协议耦合。

### 2.3 风险与缺口（影响「优解」程度）

| 风险 | 说明 | 建议 |
|------|------|------|
| **帧头冲突** | 多协议前缀相似时误匹配 | 每端口配置**插件优先级** + `minProbeLength`；冲突协议优先**分端口** |
| **嗅探缓冲区** | 半包时 `detect()` 可能反复失败 | 嗅探阶段使用**累积 ByteBuf**，未达下限不判定失败 |
| **Spring 与 Netty 线程模型** | 在 EventLoop 中调 Spring Bean 易阻塞 | 插件**启动期注册**；热路径用无锁 Registry；重逻辑丢业务线程池 |
| **会话有状态** | Session 在内存，扩缩容/宕机需重连 | 明确**无状态数据面**边界；设备 ID 映射、下行指令需**路由或粘性**方案 |
| **下行链路未展开** | 文档偏重上行 | 补充 Kafka 消费 → 会话查找 → Encoder 下发的**指令通道** |
| **安全** | 未写 TLS/mTLS、限流、黑白名单 | 生产必备：传输加密、连接速率限制、嗅探失败审计 |
| **ACK 策略** | 「Kafka 成功后再 ACK 设备」 | 正确性高、延迟大；需按协议区分**同步 ACK / 异步 ACK / 仅日志** |
| **Modbus 等同端口** | 与 JT808 等同端口嗅探场景少 | 文档示例保留，实施时**默认分端口**更稳妥 |

### 2.4 替代方案对比（何时不必用本方案）

```
                    复杂度 / 灵活性
                           ▲
     配置驱动多协议嗅探网关 │  ● OmniGateway（本文）
                           │
     每协议独立端口 + 固定   │      ● 更简单、性能可预测
     Decoder（无嗅探）       │
                           │
     独立网关进程 / 边车     │  ● Go/Rust 单机资源更省
     按协议拆分              │
                           └──────────────────────────► 运维与生态成本
```

| 场景 | 更优选择 |
|------|----------|
| 每协议固定端口、无混用 | **去掉嗅探**，端口直接绑定 Plugin，代码与延迟更简单 |
| 极致单机连接数、内存敏感 | 评估 **Netty 裸应用** 或 **Rust/Go 网关**，Java 仍可做但需严格堆与 GC 调优 |
| 团队无 Java/ Spring | 不宜强行上本栈；协议插件思想可迁移 |

**综合判断**：在「Java + Spring + Kafka + 多协议接入 + 企业运维」约束下，本方案属于**务实且可演进的主路径**；若将「优解」定义为全局最优，则需在**分端口简化、下行、会话、安全**补全后，再谈生产级优解。

---

## 3. Maven 模块划分

| 模块 | 职责 |
|------|------|
| **omni-core** | 统一设备消息模型（Thing Model）、`ProtocolPlugin` 接口与公共类型 |
| **omni-network** | Netty 封装：连接、读流、粘拆包、心跳/空闲检测、嗅探与 Pipeline 编排 |
| **omni-protocols** | 协议插件库（如 JT808、Modbus、自定义协议） |
| **omni-bootstrap** | Spring Boot 启动、配置中心集成、插件装配、上行转发 MQ |

---

## 4. 核心机制

### 4.1 嗅探与路由

- 连接建立后，Pipeline 首部为 **`SniffHandler`**。
- 按端口配置加载候选 `ProtocolPlugin` 列表，对累积缓冲区调用 `detect(buffer)`。
- 匹配成功：注入该协议的 Codec / 业务 Handler，**移除 SniffHandler**（一次嗅探，终身绑定）。
- 匹配失败或超限：按防卫策略关闭连接（见 §6.2）。

### 4.2 插件注册

- 各插件实现 `ProtocolPlugin`，由 Spring 收集为 `List<ProtocolPlugin>`（或显式 `@Bean` 注册表）。
- **禁止**在 EventLoop 中做扫描或反射；注册结果在启动时固化为只读列表。

### 4.3 插件接口职责（统一约定）

| 能力 | 说明 |
|------|------|
| 特征探测 `detect` | 基于帧头/长度字段判断是否为本协议 |
| 粘拆包 | 协议专属 `ByteToMessageDecoder` / 帧界 |
| 鉴权 | 连接建立后、业务态前的身份校验 |
| 编解码 | 字节流 ↔ 协议消息对象 |
| 物模型转换 | 协议对象 → Thing Model JSON |

---

## 5. 方案架构（系统分层）

系统划分为 **五个层级** + **旁路支撑**。

### 5.1 分层图

```
=======================================================================================
[ 5. 业务应用层 ]  设备状态 | 实时报警 | 数据分析 | 固件 OTA
======================================== ▲ ============================================
                                         │ 订阅标准 JSON / 下发控制指令
======================================== ▼ ============================================
[ 4. 消息总线层 ]           Kafka / RabbitMQ / RocketMQ
======================================== ▲ ============================================
                                         │ Thing Model JSON
==========┌──────────────────────────────┴──────────────────────────────┐==============
          │                     OmniGateway 核心层                        │
          │  ┌──────────────┐  ┌─────────────────┐  ┌──────────────────┐  │
[ 3. 网关 │  │ 多端口监听器 │→ │ 动态协议嗅探器  │→ │ 标准协议转换     │  │
  核心层 ]│  │ Port Binding │  │ Sniff & Route   │  │ Thing Model      │  │
          │  └──────────────┘  └─────────────────┘  └──────────────────┘  │
          │                          │                                        │
          │  ┌────────────────────────────────────────────────────────────┐  │
          │  │ 协议插件库: JT808 | Modbus | 自定义 | GB28181 | ...         │  │
          │  └────────────────────────────────────────────────────────────┘  │
==========└─────────▲─────────────────────▲───────────────────▲─────────┘==============
                    │                     │                   │
====================▼=====================▼===================▼========================
[ 2. 负载均衡层 ]              L4（LVS / Nginx Stream / F5）
======================================== ▲ ============================================
[ 1. 设备接入层 ]        车载终端 | 监控设备 | 工业 PLC / 传感器
=======================================================================================

【旁路支撑】
 - 配置中心 (Nacos / Apollo): 端口-协议映射、密钥、嗅探阈值
 - 监控 (Prometheus / Grafana): 连接数、QPS、嗅探失败率、解析成功率
```

### 5.2 模块说明

1. **多端口监听器**：按配置动态启动 TCP Server；例如 8081 允许协议 A/B，8082 仅协议 C。
2. **动态协议嗅探器**：连接初态不假设协议；在端口允许的插件集合内做特征匹配。
3. **协议插件库**：探测、粘包、鉴权、编解码均封装在插件内。
4. **标准协议转换**：对上仅输出带设备 ID、协议类型、时间戳、Payload 等的 **Thing Model JSON**。

---

## 6. 流程架构（连接生命周期）

### 6.1 状态机流程图

```
[ 客户端 ]                                      [ OmniGateway ]
      │                                                │
      │ 1. TCP 三次握手                                 │ (ACCEPTED)
      ├───────────────────────────────────────────────►│
      │ 2. 首包（含帧头）                               │ 分配嗅探缓冲，启动嗅探定时器
      ├───────────────────────────────────────────────►│ (SNIFFING)
      │                                                ├─► 长度 < 探测下限 → 继续读
      │                                                ├─► 遍历 plugin.detect(buffer)
      │                                                │     ├─ 失败 → 关闭 / 累计至上限
      │                                                │     └─ 成功 → (BINDING)
      │                                                ├─► 移除 Sniffer，注入协议 Codec
      │                                                ├─► 协议鉴权
      │ 3. 鉴权响应（按需）                             │ (CONNECTED) + Session + 心跳
      ◄───────────────────────────────────────────────┤
      │ 4. 业务数据（可能粘包/半包）                     │
      ├───────────────────────────────────────────────►├─► Decoder → Message → Thing Model
      │                                                ├─► 异步发 Kafka
      │ 5. 业务 ACK（按需）                             ├─► MQ 确认策略 → Encoder 回写
      ◄───────────────────────────────────────────────┤
      │ 6. FIN / 心跳超时 / 鉴权失败                    │ (CLOSED)
      ├───────────────────────────────────────────────►├─► 销毁 Session、释放缓冲、离线事件
```

### 6.2 关键机制

#### 嗅探防卫（Sniff & Drop）

- **问题**：空连、乱码占满连接与内存。
- **策略**：`SNIFFING` 阶段强制 `Max_Sniff_Bytes`（如 256）与 `Sniff_Timeout`（如 5s）；超限即 RST，不进入业务态。

#### 动态 Pipeline 更替

- 嗅探前：`TCP → 缓冲 → Sniffer`
- 绑定后：`TCP → 协议拆包 → 解码 → 业务处理`（Sniffer 已移除，稳态零嗅探开销）

#### 背压（Backpressure）

- **问题**：Kafka 或下游变慢，网关解析仍快，内存膨胀。
- **策略**：发送受阻时 `channel.config().setAutoRead(false)`，利用 TCP 窗口自然反压设备端；恢复后重新 `setAutoRead(true)`。

---

## 7. 技术栈

| 类别 | 选型 |
|------|------|
| 语言 | Java 17 / 21 |
| 网络 | Netty（NIO、ByteBuf） |
| 应用框架 | Spring Boot |
| 配置中心 | Nacos / Apollo |
| 消息总线 | Kafka（推荐；亦可 RabbitMQ / RocketMQ） |
| 监控 | Micrometer + Prometheus + Grafana |

---

## 8. 详细设计（待确认）

以下六项已展开为独立详细设计，**确认前不进入开发**。索引与依赖见 [docs/design/README.md](docs/design/README.md)。

| # | 主题 | 文档 | 状态 |
|---|------|------|------|
| 01 | 下行通道（MQ → Session → 编码下发） | [01-downlink-channel.md](docs/design/01-downlink-channel.md) | 待确认 |
| 02 | 水平扩展（本地会话 / Redis 路由 / drain） | [02-horizontal-scaling.md](docs/design/02-horizontal-scaling.md) | 待确认 |
| 03 | 传输安全（TLS、mTLS、限流、证书轮换） | [03-transport-security.md](docs/design/03-transport-security.md) | 待确认 |
| 04 | 可观测性（Metrics、日志、Tracing） | [04-observability.md](docs/design/04-observability.md) | 待确认 |
| 05 | 配置热更新（分级热更、端口 drain） | [05-config-hot-reload.md](docs/design/05-config-hot-reload.md) | 待确认 |
| 06 | SLO 与容量（基线、压测、扩容公式） | [06-slo-metrics.md](docs/design/06-slo-metrics.md) | 待确认 |

### 分期实施

完整分期说明（目标、交付清单、里程碑、验收标准）见 **[docs/PHASES.md](docs/PHASES.md)**。

| 阶段 | 一句话 |
|------|--------|
| **第一期 MVP** | 接入 + 嗅探 + 1 个协议 + 上下行 Kafka + 本地 Session + 基础监控 + 基础压测 |
| **第二期 生产增强** | Redis 下行路由 + TLS/mTLS + 配置/证书热更 + drain + Tracing + 全量压测与 SLO 门禁 |

### 如何确认

对每份文档末尾 **「待确认清单」** 回复 `同意默认` 或逐项修改；也可一次性回复 **「全部同意建议默认」** 后开始排期开发。

---

*文档版本：含详细设计 v1；实现代码以确认后里程碑为准。*
